# 🚀 快速创建 GitHub 仓库并推送代码

## 📋 准备工作

代码已经提交到本地仓库，现在只需要：
1. 在 GitHub 上创建两个空仓库
2. 推送代码到 GitHub

## ⚡ 最快方法（推荐）

### 步骤 1：在 GitHub 网页创建仓库

#### 创建前端仓库

1. 打开浏览器，访问：https://github.com/new
2. 填写信息：
   - **Repository name**: `hospital-frontend`
   - **Description**: `医院预约挂号系统 - 前端应用 (Vue 3 + Element Plus)`
   - **Visibility**: 选择 Public 或 Private
   - **不要勾选**任何初始化选项（README、.gitignore、license）
3. 点击绿色的 **"Create repository"** 按钮

#### 创建后端仓库

1. 再次访问：https://github.com/new
2. 填写信息：
   - **Repository name**: `hospital-appointment-system`
   - **Description**: `医院预约挂号系统 - 后端服务 (Spring Boot + MyBatis-Plus)`
   - **Visibility**: 选择 Public 或 Private
   - **不要勾选**任何初始化选项
3. 点击绿色的 **"Create repository"** 按钮

### 步骤 2：推送代码

创建完仓库后，**双击运行** `push-to-github.bat` 文件，脚本会自动推送代码。

或者手动在 Git Bash 中执行：

```bash
# 推送前端
cd hospital-frontend
git push -u origin main

# 推送后端
cd ../hospital-appointment-system
git push -u origin main
```

## 🔐 如果遇到认证问题

### 方法 1：使用 Personal Access Token（推荐）

1. 访问：https://github.com/settings/tokens
2. 点击 "Generate new token" -> "Generate new token (classic)"
3. 填写 Note：`推送代码`
4. 勾选权限：`repo`（完整仓库权限）
5. 点击 "Generate token"
6. **复制token**（只显示一次）

推送时，当提示输入密码时，**粘贴token而不是密码**。

### 方法 2：配置 Git Credential Manager

```bash
git config --global credential.helper manager-core
```

然后正常推送，会弹出窗口让您登录。

### 方法 3：在 URL 中包含 token

```bash
cd hospital-frontend
git remote set-url origin https://YOUR_TOKEN@github.com/XDD513/hospital-frontend.git
git push -u origin main
```

## ✅ 验证推送成功

推送成功后，访问以下地址查看：

- 前端：https://github.com/XDD513/hospital-frontend
- 后端：https://github.com/XDD513/hospital-appointment-system

您应该能看到所有代码文件。

## 🆘 常见问题

### 1. "repository not found" 错误

**原因**：仓库还未创建或名称不正确

**解决**：确保在 GitHub 上已经创建了仓库，名称完全匹配

### 2. "authentication failed" 错误

**原因**：Git 认证失败

**解决**：
- 使用 Personal Access Token 而不是密码
- 检查 token 权限是否包含 `repo`
- 使用 `git config --global credential.helper manager-core` 重新配置

### 3. "connection reset" 或网络错误

**原因**：网络连接问题

**解决**：
- 检查网络连接
- 使用代理或 VPN
- 稍后重试

### 4. 仓库已存在但推送失败

**原因**：远程仓库有内容（如 README）

**解决**：
```bash
# 先拉取远程内容
git pull origin main --allow-unrelated-histories

# 解决冲突后推送
git push -u origin main
```

## 📞 需要帮助？

如果遇到问题，请检查：

1. ✅ 仓库是否已在 GitHub 上创建
2. ✅ Git 是否已配置用户名和邮箱
3. ✅ 网络连接是否正常
4. ✅ 是否使用了正确的认证方式（token）

## 🎉 完成后的下一步

推送成功后，您可以：

1. 在 GitHub 上查看代码
2. 设置仓库描述、标签和主题
3. 配置 GitHub Actions 进行 CI/CD
4. 邀请协作者
5. 创建 Issues 和 Pull Requests

---

**提示**：整个过程大约需要 5-10 分钟，主要是创建仓库和推送代码的时间。

